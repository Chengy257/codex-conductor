interface:
  display_name: "Codex Conductor"
  short_description: "GPT-6 Sol directs; GPT-6 Luna explores, implements, and tests"
  default_prompt: "Use $codex-conductor with my selected GPT-6 Sol root session as director. Offload coherent repository investigation, implementation, and focused verification to GPT-6 Luna roles when useful. Verify effective model routing and keep final acceptance in Sol."

policy:
  allow_implicit_invocation: false
